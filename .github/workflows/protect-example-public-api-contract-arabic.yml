# This file is generated by scripts/sync-flow-workflows.js. Do not edit manually.
# Source profile: ProtectExamplePublicApiContractArabic
# Source suite: flows/examples/wewill/ProtectExamplePublicApiContractArabic.xml
name: ProtectExamplePublicApiContractArabic

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up JDK 11
        uses: actions/setup-java@v4
        with:
          java-version: '11'
          distribution: 'temurin'
          cache: maven

      - name: Run API Tests
        run: mvn clean test -P ProtectExamplePublicApiContractArabic

      - name: Upload test report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-report
          path: ./extent-reports/
