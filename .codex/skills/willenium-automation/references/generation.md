# Generation Workflow

Generate Playwright artifacts from approved plans.

- keep tests focused on one business expectation
- keep user-facing values in `src/pw/config/testdata/...`
- update existing owner assets before creating duplicates
- keep flow ownership mapping in `flows-ts/...`
