# Jira MCP

Use Jira MCP at the agent layer for:

- reading bug context before planning
- updating issue comments with evidence
- linking plan/test artifact paths

Do not embed Jira calls in test runtime code.
