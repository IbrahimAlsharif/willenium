# Verification Workflow

Run the relevant Playwright path for changed coverage and fix failures before stopping.

Typical commands:

- `npm run pw:typecheck`
- `npm run pw:test`
- profile-specific `npm run pw:test:home:...`
